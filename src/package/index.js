import PackageIcon from '@material-ui/icons/Collections';
import PackageList from './PackageList';
import PackageEdit from './PackageEdit';
import PackageCreate from './PackageCreate';

export default {
    list: PackageList,
    create: PackageCreate,
    edit: PackageEdit,
    icon: PackageIcon,
};
